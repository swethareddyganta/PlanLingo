{
  "name": "@daily-flow/backend",
  "version": "1.0.0",
  "description": "FastAPI backend for Daily Flow application",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "python -m pytest tests/",
    "lint": "python -m flake8 app/ tests/",
    "format": "python -m black app/ tests/"
  },
  "keywords": [
    "fastapi",
    "python",
    "api",
    "daily-flow"
  ],
  "private": true
}
